// This is a test file to verify that our pre-commit hook works correctly
function testFunction() {
  const unformattedVariable = 'This is not formatted correctly';

  return unformattedVariable;
}

export default testFunction;
